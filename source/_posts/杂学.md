---
title: 杂学
date: 2023-12-01 01:23:56
tags:
  - Pseudo-tech
categories:
  - Tech
---

## Videos

### yt-dlp

- 从文件读取 `-a`
- 获取所有可用轨道 `-F`
- 选中轨道 `-f`
- `--cookies cookies.txt` `--cookies-from-browser edge`
- **abema.tv**: 不使用cookie，直接带用户名和密码 `yt-dlp 'https://abema.tv/video/episode/xxx' --username 'xxx' --password 'xxx'` 如果报错就清空 `/.cache/yt-dlp`
- `--playlist-items 1-5` `--playlist-items 1,3,5`

### 实战：bili.tv

1. 泰国 vpn，比如 urban vpn
2.

```bash
yt-dlp --cookies cookies.txt --add-header "Referer: https://www.bilibili.tv/" target_url
```

### minyami

1. 带 cookies

```bash
minyami --headers "Referer: https://www.bilibili.tv/" -H "Cookie:cookies_text" -d "xxx.m3u8" --output "xxx.ts"
```

2. ~~设置代理~~
   ~~不要使用环境变量，直接开启代理软件的 TUN 模式（增强模式）~~

### handbrake 压缩常用配置

Mac mini M2

- mkv/mp4
- H.265 10-bit (x265)
- RF: 18-16
- FPS: Same as Source（Constant）
- Preset: medium/slow（这个才是最大因素）
- Level: 4.1
- Profile: main10
- 音频比特率：passthru/256

### ffmpeg

1. 無損切割

```bash
ffmpeg -i XXX.mp4  -ss 00:00:00 -to 00:00:00 -c copy output.mp4
```

2. 無損拼接

   `filelist.txt`

```bash
file '01.mp4'
file '02.mp4'
file '03.mp4'
file '04.mp4'
```

```bash
ffmpeg -f concat -safe 0 -i filelist.txt -c copy output.mp4
```

3. 抽軌
   `MKVToolNix`
   ＊抽音軌

   ```bash
   ffmpeg -i <input> -vn -c:a copy output_audio.opus
   ```

   ＊抽字幕

   ```bash
   ffmpeg -i <input> -map 0:2 output_simplified_chinese.ass
   ```

   ＊抽圖片

   ```bash
   ffmpeg -i <input> output%d.png
   ```

4. 合轨

   `MKVToolNix`

   or

```bash
ffmpeg -i video.m2v -i audio.adv -c:v copy -c:a copy output.mkv
```

5. 调轴（音画不同步）

```bash
ffmpeg -i your_video.mp4 -itsoffset delay -i your_video.mp4 -map 0:v -map 1:a -c copy output.mp4
```

&emsp;&emsp;`delay` 是延迟时间（以秒为单位）`1.5` `-1.5`

### 实战：mygo MV

webm to m4a lossless

```bash
ffmpeg -i input.webm -vn -c:a alac output.m4a
```

### 实战：audee.jp

1. 猫抓嗅探 m3u8 → 解析，获取 KEY(hex)

2.

```bash
minyami -d 'XXX.m3u8' --key '234d073d2091593011e02546e8888888'
```

### 实战：蓮

1. 抓包，获取 usm 文件。iOS 用 stream，macOS 用 Surge。
   ＊[Surge 抓包教程](https://surge.mitsea.com/http-processing/mitm)
   ＊简要步骤：安装根证书 → 勾选 `tcp-connection` `skip-server-cert-verify` → 开启流量捕获 + mitm 解密 → 指定解密的流量 `*.icloud.com`

2. vgmtoolbox_bin_r1040

3. 選擇 Msic. Tool > Stream Tools > Video Demultiplexer，將格式選擇為USM，並把檔案拉進去，等待結果

4. adx 音频格式要转码

```bash
ffmpeg -i input.adx -c:a flac output.flac
```

5. 合轨至 mkv

```bash
ffmpeg -i v.m2v -i a.flac -c:v copy -c:a copy out.mkv
```

### 实战：stagecrowd

1. 提取CDM

   ＊Root 的 Android 11 以下的手機
   ＊安裝frida
   ＊dumper →　`.bin` `.pem` → `.wvd`

2. 寻找 `PSSH` `Licence url`，构造请求，获取`KEY`

3. 使用`N_m3u8DL-RE` + `shaka-packager`解密（此步未经验证）

   示例命令：

   ```bash
   ./N_m3u8DL-RE -M format=mp4 "https://cdn.bitmovin.com/content/assets/art-of-motion_drm/mpds/11331.mpd" --key "key1" --key "key2"  --use-shaka-packager -sv best -sa best
   ```

### 实战：eplus

1. `PSSH`，在mpd文件中。

2. `licence url`，控制台里面看到的不对，在网上搜寻到正确的url。需要准备`X-Dt-Auth-Token`和`Dt-Custom-Data`字段。实测`Dt-Custom-Data`可以直接从线上扒。

3. 得到`key`。

4.

```bash
N_m3u8DL-RE -M format=mp4 "XXX.mpd" -H "Cookie:XXX" --key XXX --use-shaka-packager --save-name eplus.mp4 -sv best -sa best
```

### 实战：radiko

m3u8 确实不全，只能拼时间。→ `yt-dlp`
注意 Geo Restrict

### 实战：lemino直播

如果你手头已经有加密的视频和key，只希望在本地解，怎么办？

```bash
mp4decrypt --show-progress --key AAA:BBB input.m4a output.m4a
```

### 实战：asobi channel

```bash
yt-dlp -f worst 'https://asobichannel.asobistore.jp/watch/8eedhfu6ff'
```

### 实战：mygo BD

```bash
ffmpeg -i input.mkv -map 0:a:0 -c:a alac output.m4a
```

後ChatGPTに聞く

```
我现在有这样一个视频：
ffmpeg -i output.m4a
Input #0, mov,mp4,m4a,3gp,3g2,mj2, from 'output.m4a':
  Metadata:
    major_brand     : M4A
    minor_version   : 512
    compatible_brands: M4A isomiso2
    title           : MyGO!!!!! 6th Single「聿日箋秋」【Blu-ray付生産限定盤】DISC1
    encoder         : Lavf61.7.100
  Duration: 01:38:33.91, start: 0.000000, bitrate: 1766 kb/s
  Chapters:
    Chapter #0:0: start 0.000000, end 78.620000
      Metadata:
        title           : Chapter 01
  Stream #0:0[0x1](jpn): Audio: alac (alac / 0x63616C61), 48000 Hz, stereo, s32p (24 bit), 1766 kb/s (default)
      Metadata:
        handler_name    : SoundHandler
        vendor_id       : [0][0][0][0]
  Stream #0:1[0x2](eng): Data: bin_data (text / 0x74786574)
      Metadata:
        handler_name    : SubtitleHandler
At least one output file must be specified

请你按照每个章节切片，帮我生成所有的切片命令。比如：ffmpeg -i /Users/zongliang/output.m4a -c copy -ss 0.000000 -to 78.620000 1.m4a
```

### 实战：bilibili

```
brew install aria2
--downloader aria2c
```

### 实战：niconico生放送（タイムシフト）

直接`yt-dlp`使うとエラー出てる。
`minyami`だと遅い。
結局
`N_m3u8DL-RE`

### burn blu-ray

### 实战：aria下载onedrive

1. Network 提取真实下载链接（根据size判断）
2. 复制为cURL，用脚本转换为aria2c命令。

### MakeMKV

1. Download latest KEYDB: http://fvonline-db.bplaced.net/
2. /Users/<username>/Library/MakeMKV

## Comics

### epub to zip

1. epub → docx (Calibre)
2. 打开，随便删除一个文字
3. 重命名为 zip，解压找到 media 文件夹

## Start with Mac mini M2

### dock 在全屏时不自动隐藏

```bash
defaults write com.apple.dock autohide-delay -int 0
defaults write com.apple.dock autohide-time-modifier -float 1.0
killall Dock
```

### 连接蓝牙耳机卡顿

1. 耳机断联
2. `sudo defaults write bluetoothaudiod "Enable AAC codec" -bool true`
3. 设置→声音→输入，取消选中耳机

### 睡眠时异常唤醒硬盘

1. 查询电源设置

```bash
pmset -g
```

2. 修改某个选项

```bash
sudo pmset -a tcpkeepalive 0
sudo pmset -a powernap 0 # Apple 芯片的 Mac 上，此值默认为 1
```

3. It should be like this:

```bash
  System-wide power settings:
  Currently in use:
    disksleep            10
    powernap             0
    womp                 1
    networkoversleep     0
    sleep                1 (sleep prevented by powerd, mds_stores)
    Sleep On Power Button 1
    ttyskeepawake        1
    tcpkeepalive         0
    autorestart          0
    standby              0
    displaysleep         10
```

### Kill Microsoft AutoUpdate

```bash
/Library/Application Support/Microsoft/
```

### 蘋果死活不做的內建功能

- Mos
- BetterDisplay
- Spectangle
- AppCleaner

### 重建索引

1. 关闭索引

```bash
sudo mdutil -a -i off
```

2. 重建索引

```bash
sudo mdutil -a -E
```

3. 打开索引

```bash
sudo mdutil -a -i on
```

检视活动监视器以查看索引状态。

### 關閉 sonoma 的 TextIndicator

```bash
defaults write kCFPreferencesAnyApplication TSMLanguageIndicatorEnabled 0
```

### iOS更新文件下载位置

```
~/Library/iTunes/
```

## 开发

### 1. hosts

```bash
sudo vi /etc/hosts
```

vs codeも使える

```bash
code /etc/hosts
```

### 2. https服务器

```bash
brew install mkcert
mkcert -install
mkdir ~/cert
cd cert
mkcert localhost 127.0.0.1 192.168.11.2
ls
```

```bash
% http-server -c-1 . --ssl --key ~/cert/localhost+2-key.pem --cert ~/cert/localhost+2.pem --port 443 --cors
```
